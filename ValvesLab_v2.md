# ValveLab v2：执行机构与流量闭环实验

状态：已完成实现、模型验证、浏览器检查及生产构建。日期：2026-09-05。实际验证范围见 [VALIDATION_v2.md](./VALIDATION_v2.md)。

## 1. 先修正 v1 流线

用户截图中，闸阀上游的三条路径从上到下排列，但节流口的路径目标高度反向排列，造成可见交叉。这是路径生成错误，不能解释为本二维视图中的真实流线。

在同一时刻、速度场连续且速度非零的二维流动区域，流线沿当地速度方向，不能以不同方向相交。三维投影、不同时间的轨迹，以及驻点等特殊情形不应被用来解释本图的问题。本图没有声明这些条件。

修复采用共同横坐标采样的流带：每个截面定义有序的上、下边界，各路径采用固定比例插值。所有线在全过程保持上下次序；对五种阀门进行全开度采样，检查严格向右及无路径交换。保留实体遮罩和关闭停流规则。路径仍是教学示意，不声称求解了速度场或连续方程。

## 2. 目标与范围

在 v1 的五种阀门、静态模型、剖视图、曲线与说明基础上增加四项能力：

1. 电动 / 气动执行机构，模拟响应迟滞、行程速度限制和不同失能行为。
2. 手动指令 / PID 自动流量控制，观察设定值与测量值的关系。
3. 入口、出口压力变化，并观察压差扰动与控制补偿。
4. 阀杆卡死、驱动能源中断、阀座泄漏、传感器偏差的可注入和可清除故障。

仍为纯本地 React + TypeScript + Vite 前端，无后端、真实设备连接、CFD、真实电机电磁模型或气动热力学求解。沿用依赖与本地运行方式，不创建 Git 提交或外部部署。

## 3. 页面组织

- 顶部增加结构学习 / 闭环实验两个模式。结构学习保留 v1 的直接开度操作。
- 闭环实验中，剖视图由执行机构实际开度驱动；右侧显示手动/PID、设定值或手动指令、实际开度、真实流量与测量流量。
- 增加执行机构示意图、指令与反馈读数、信号链及实时趋势。
- 下方三类实验设置：执行机构、压差、故障；PID 参数放在控制面板。
- 实时趋势分开呈现流量（L/min）和开度（%），避免双轴单位混淆。显示最近 60 秒，保留 SP、真实流量、测量流量、指令开度和实际开度。
- 提供开始/暂停、单步 0.05 秒、重置实验。编辑配置不自动开始运行；暂停时配置被暂存，下一步才推进系统状态。
- 切换阀门将动态配置、时间和历史重置为该阀默认值，清除故障。模式之间切换暂停动态实验；手动结构学习状态与动态状态分别保留。重置实验按钮保留当前配置，清除故障，将实际开度回到 65%，时间归零，历史回到初始点。
- 说明普通闸阀/球阀参加闭环实验是为了教学比较，不意味着适合工业连续节流。

## 4. 物理量与简化过程模型

采用固定水样介质，相对密度为 1。压力为表压（bar），仅使用压差；入口和出口压力可独立设置为 0～4 bar。当前版只求解正向流动；非正压差时正向流量为零，并显示该模型边界。

基准压差为 1 bar，所有阀门采用统一的示例全开流量 100 L/min。这是比较控制响应的公共教学标尺，不是五种真实阀门具有相同通流能力。

令 k(h) 为 v1 的归一化流量特性，h 为实际归一化开度。健康时：

`Q = 100 × k(h) × sqrt(max(Pin − Pout, 0) / 1 bar)`，单位 L/min。

它是不可压缩介质的简化准稳态压差关系，不包含气蚀、闪蒸、阻塞流、温度或管路动态。实际流量可超过基准 100 L/min，界面不得硬截断为 100%。压力与开度决定每个时间步的真实流量，传感器另有一阶延迟。

发生阀座泄漏时，采用 `k_eff = leak + (1 − leak) × k(h)`，leak 范围 1～15%。关闭时仍存在泄漏流量；正常零开度和零正向压差仍为零流量。泄漏用单独的放大示意/提示标识，不能把主阀门画成已经打开。

传感器目标为 `max(0, Q + bias)`，bias 仅在传感器偏差故障期间生效；测量值以 0.2 秒时间常数一阶跟随该目标。PID 使用测量值，真实流量同时显示，便于观察控制器被偏差误导。

## 5. 执行机构

统一输入为命令开度 u（0～100%），输出为实际开度 x（0～100%）。每步按一阶目标响应计算位移，并限制最大行程速度。距离目标小于 0.001 个百分点时到达目标，避免数值渐近导致关闭后保留微小流量。

| 类型 | 默认时间常数 | 默认最大速度 | 能源中断时的行为 |
| --- | --- | --- | --- |
| 电动 | 0.8 s | 12 %/s | 本例为无备用能源的保持原位型 |
| 气动 | 0.35 s | 35 %/s | 本例为单作用弹簧复位，返回选定的失气关/失气开位置 |

时间常数可调 0.1～3 s，最大速度可调 2～60 %/s。这些数值为示例，不代表所有电动/气动产品。气动能源中断时使用独立的弹簧复位响应（0.6 s、25 %/s）；机械卡死优先保持实际开度不变。每次只激活一种故障，避免未定义的故障叠加。

旋转阀将执行机构行程映射为角度，直行程阀映射为升程；示意图显示实际运动及目标位置，阀门剖视跟随实际位置而非控制命令。

## 6. PID

并联形式：`u_raw = Kp × (SP − PV) + I − Kd × filtered(dPV/dt)`。

- SP：目标流量，范围 0～150 L/min。
- PV：传感器测量流量。
- u：输出限幅到 0～100% 的开度命令。
- 默认 Kp=1.1、Ki=0.55、Kd=0.04；微分滤波时间常数 0.15 s。
- Kp 单位 %/(L/min)，Ki 单位 %/((L/min)·s)，Kd 单位 %·s/(L/min)。界面提供 P、I、D 贡献。
- 微分作用于测量值，避免设定值阶跃引起微分冲击。
- 条件积分抗饱和：输出饱和且误差继续推动同方向时停止积分；临近边界时只接受到达输出边界所需的积分增量。卡死或能源中断时冻结积分。积分状态另设 ±2000 的明确数值保护限幅。
- 手动切自动及在线修改增益时，重算积分初值以保持当前输出；自动切手动将当前命令写入手动指令，避免开度命令跳变。
- 显示输出受限及设定值超出当前正向通流能力的提示，不声称 PID 能克服物理容量限制。

## 7. 故障实验

| 故障 | 注入后模型行为 | 学习观察 |
| --- | --- | --- |
| 阀杆卡死 | 实际开度保持注入时位置 | 命令与实际位置分离 |
| 电源/气源中断 | 电动保持原位；气动按设定弹簧复位 | 同一控制命令不能保证相同失能行为 |
| 阀座泄漏 | 最小有效通流系数非零 | 关闭命令与零流量不是同一件事 |
| 传感器偏差 | PV 目标加入指定偏差，真实 Q 不直接改写 | 控制器可能跟踪错误测量 |

故障可清除，恢复过程从当前实际状态继续，不瞬时重置开度。事件记录显示配置变化、故障注入/清除的仿真时刻，保存最近 12 条。

## 8. 时间与数据架构

- 纯函数固定时间步长 0.05 s，可在 Node 中不依赖浏览器复现。
- 浏览器以 requestAnimationFrame 驱动固定步累积；后台隐藏时暂停物理时间，返回后不追赶后台时间。
- UI 约 10 Hz 刷新，历史约 5 Hz 采样，内存上限 301 个点，图表展示最近 60 s。
- 图形、真实流量、读数和趋势使用同一个仿真状态；动画强度单独归一化且限幅，不能限制真实流量。
- `simulation/types.ts`：配置与状态。
- `simulation/engine.ts`：执行机构、过程、传感器、PID 及配置切换。
- `simulation/useSimulation.ts`：时钟、暂停、历史、事件及 React 绑定。
- `components/SimulationControl.tsx`、`ActuatorView.tsx`、`SimulationSettings.tsx`、`TrendChart.tsx`：动态界面。
- `valves/geometry.ts`：不交叉的有序流带与路径输出。

## 9. 实施与验收

1. 写完本文，修复五种阀门流线并验证用户截图中的 79.5% 闸阀工况。
2. 实现独立仿真引擎和模型验证。
3. 接入执行机构、PID、压力、故障与趋势界面。
4. 浏览器检查真实操作、暂停/单步、故障恢复和响应式布局，完成生产构建。
5. 更新 README 与 v2 验证记录。

验收包括：有序流线无交叉；相同开度下 4 倍压差产生 2 倍流量；执行机构不瞬时跳到目标且满足速度上限；PID 默认参数在健康可达工况收敛；手动/自动切换无命令阶跃；饱和与长时间故障不造成积分无限增长；四种故障可注入及清除；关闭泄漏工况明确呈现；暂停及后台不推进时间；历史数量有界；旧学习功能仍可用；移动布局无整页溢出。

## 10. 参考依据

- [NASA：Definition of Streamlines](https://www1.grc.nasa.gov/beginners-guide-to-aeronautics/definition-of-streamlines/)：流线与局部速度相切，流体不穿越流线。
- [MathWorks：Anti-Windup Control](https://www.mathworks.com/help/simulink/slref/anti-windup-control-using-a-pid-controller.html)：输出限幅及条件积分/跟踪方法。
- [MathWorks：Bumpless Control Transfer](https://www.mathworks.com/help/simulink/slref/bumpless-control-transfer-between-manual-and-pid-control.html)：手动与自动控制之间的输出跟踪。
- [Spirax Sarco：Control Valve Capacity](https://www.spiraxsarco.com/learn-about-steam/control-hardware-electric-pneumatic-actuation/control-valve-capacity)：压差与液体流量系数关系。
- [Spirax Sarco：Control Valve Actuators and Positioners](https://www.spiraxsarco.com/learn-about-steam/control-hardware-electric-pneumatic-actuation/control-valve-actuators-and-positioners)：电动、气动执行机构及失能行为。

资料用于核对原理，不用于提供产品参数。本文默认参数、动态时间常数、泄漏系数与控制增益均为本项目自选教学值。

## 11. 执行结果

以上功能已接入本地网页。五种阀门的 5,000 个非零开度流线工况、28,200 个动态时间步验证通过；原有 5,005 个静态模型采样继续通过。浏览器完成 37 项动态实验检查、117 项旧功能回归和最终生产版本的 11 项补充检查，TypeScript 检查与 Vite 生产构建通过。

使用步骤见 [README.md](./README.md)，测试命令、截图、验证边界与复现脚本见 [VALIDATION_v2.md](./VALIDATION_v2.md)。
